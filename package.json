{
  "name": "partykit-chat-sample",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "hono-party": "^1.0.0",
    "partyserver": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
